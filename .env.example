# SMTP configuration for contact form
# Primary keys
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Optional aliases supported by the server (use if you prefer):
# SMTP_SERVER= smtp.example.com            # alias of SMTP_HOST
# EMAIL_SENDER= no-reply@example.com       # alias of SMTP_FROM
# EMAIL_RECEIVER= contact@example.com      # alias of SMTP_TO

# Optional behavior flags
# SMTP_SECURE=true                         # force SMTPS (465). Default: true if port=465 else false
# SMTP_BCC= copies@example.com             # receives a blind copy of every contact email

# Sender and recipient (fallbacks to SMTP_USER if unset)
SMTP_FROM=Taolenn Assistant <no-reply@example.com>
SMTP_TO=contact@example.com

# Web server port (optional)
# PORT=3000
